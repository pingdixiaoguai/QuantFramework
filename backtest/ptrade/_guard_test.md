# Guard test — DELETE ME

Temporary file under `backtest/ptrade/` used to verify the `no-ptrade-on-main`
GitHub Actions guard rejects PRs to main that touch PTrade-only paths.

This file and its branch/PR should be cleaned up after the test.
