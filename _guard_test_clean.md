# Guard test (clean control) — DELETE ME

Temporary file at repo root (NOT under `deploy/` or `backtest/ptrade/`) used to
verify the `no-ptrade-on-main` guard PASSES for PRs that don't touch
PTrade-only paths.

This file and its branch/PR should be cleaned up after the test.
