experiment:
  id: conditioned_defender_range_escape_final_candidate_v1
  created_on: "2026-08-27"
  evidence_status: retrospective_post_search_robust_candidate_not_independent_oos
  evaluation_start: "2019-01-18"
  evidence_cutoff: "2026-08-26"
  baseline_strategy_id: momentum_defender_w40_qm40_threshold_v5
  prior_return_panels:
    - experiments/20260827_conditioned_defender_range_escape_2019/daily_returns.parquet
    - experiments/20260827_conditioned_defender_range_escape_focused/focused_daily_returns.parquet
    - experiments/20260827_conditioned_defender_range_escape_final_axis/axis_daily_returns.parquet
    - experiments/20260827_conditioned_defender_range_escape_ensemble/ensemble_daily_returns.parquet
  ensemble_group_summary: experiments/20260827_conditioned_defender_range_escape_ensemble/ensemble_group_summary.csv

periods:
  development: ["2019-01-18", "2022-12-30"]
  validation: ["2023-01-01", "2024-12-31"]
  recent: ["2025-01-01", "2026-08-26"]
  complete_pool: ["2024-02-06", "2026-08-26"]

candidate:
  anchor_mode: fixed_512890
  range_window: 40
  upper_threshold: 0.95
  momentum_weight: 0.15
  quality_windows: [38, 40, 42]
  quality_aggregation: mean
  absolute_floor: 0.000
  relative_floor: 0.0225
  hold_policy: fixed_pulse_rearm
  hold_days: 5

one_factor_axes:
  range_window: [37, 38, 39, 40, 41, 42, 43, 44, 45]
  upper_threshold: [0.90, 0.925, 0.95, 0.975, 1.00]
  momentum_weight: [0.05, 0.10, 0.125, 0.15, 0.175, 0.20, 0.25, 0.30]
  hold_days: [3, 5, 7, 10]
  absolute_floor: [null, -0.005, 0.000, 0.005, 0.010, 0.015]
  relative_floor: [0.015, 0.020, 0.0225, 0.025, 0.0275, 0.030, 0.035]

stress_axes:
  hold_days: [15]

robustness:
  delta_epsilon: 0.000001
  minimum_full_dual_rate: 0.60
  minimum_development_dual_rate: 0.60
  minimum_validation_dual_rate: 0.60
  minimum_recent_dual_rate: 0.60
  require_full_q25_positive: true
  require_development_q25_positive: true
  require_validation_q25_positive: true
  require_recent_q25_nonnegative: true
  selected_ensemble_profile: local3
  selected_ensemble_aggregation: mean
  minimum_ensemble_threshold_full_dual_rate: 0.80
  minimum_ensemble_threshold_validation_dual_rate: 0.70
  no_automatic_production_replacement: true
  disclosed_axis_correction: >-
    The first final-axis pass accidentally overwrote the center candidate's
    source label when an axis contained the center value, so each axis omitted
    the center from its summary. The corrected catalog preserves all source
    labels. Hold day 15 remains a disclosed three-times-duration stress point;
    the local 5-day neighborhood is 3/5/7/10 and is evaluated separately.

overfit_checks:
  cscv_blocks: 12
  paired_block_bootstrap_block: 20
  paired_block_bootstrap_repetitions: 5000
  yearly_reality_check_repetitions: 5000
  random_seed: 20260827
  rolling_windows: [252, 504]
  transaction_cost_multipliers: [1.0, 3.0, 10.0]
