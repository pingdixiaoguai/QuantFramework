strategy_name: quality_momentum_top1
strategy_class: strategy.top1.Top1
asset_pool:
  - 510300.SH   # 沪深300 ETF
  - 159915.SZ   # 创业板 ETF
  - 513100.SH   # 纳斯达克 ETF
  - 518880.SH   # 黄金 ETF
start: "2013-07-01"
end: "today"
factors:
  - name: quality_momentum
    weight: 1.0
    params: {window: 20}
train_ratio: 0.7
rebalance_rule: daily      # legacy field, kept for back-compat (engine ignores it)
rebalance_days: 5          # rebalance every N trading days; reduces friction

# Independent, read-only signal configuration.  The daily runner loads this
# through the same registry/strategy pipeline as a normal strategy.
notification_shadow_configs:
  - strategy/configs/quality_momentum_top1_ohlc_er.yaml
