# 项目进展

> 本文档是 QuantFramework 进展的单一入口。`/progress` 技能读取此文件。
> 进展信息的权威来源仍是 `CLAUDE.md`（阶段状态表）、`strategy_changelog.md`（策略变更日志）、git history；本文档为它们的汇总快照。
>
> **最后更新**：2026-06-12

---

## 一、总体阶段

**框架本身（六层架构）已全部完工**，并已**迁移部署到 PTrade 平台**（实盘券商环境）。项目重心：策略研究与调优 + 实盘运行监测。

| Phase | 交付物 | 状态 |
|-------|--------|------|
| 0 | 项目脚手架 + 接口桩 | ✅ done |
| 1 | 数据层（Tushare 同步 / Parquet 存储 / 查询） | ✅ done |
| 2 | 因子层 + 标准化层 | ✅ done |
| 3 | 回测引擎 | ✅ done |
| 4 | 策略层 | ✅ done |
| 5 | 执行层 | ✅ done |
| 6 | 通知层（钉钉） | ✅ done |
| + | PTrade 迁移部署（quality_momentum_top1） | ✅ done（平台校准 + 2014–2026 回测验证） |

---

## 二、当前实盘策略

`quality_momentum_top1` —— 框架配置 `strategy/configs/quality_momentum_top1.yaml`；PTrade 部署 `deploy/ptrade_quality_momentum_top1.py`

- **资产池**：4 只 ETF —— 沪深300（510300）/ 创业板（159915）/ 纳指（513100）/ 黄金（518880）
- **信号**：单因子 `quality_momentum`(window=20)，Top1 全仓轮动
- **调仓**：`rebalance_days = 2`（min_hold）——已由 changelog §3.6 含成本回测**有据确立**
- **PTrade 校准要点**：代码后缀 `.SS`；`order_target_value` 按市值下单；A股 T+1 守卫；`set_volume_ratio(1.0)`；日线频率

---

## 三、近期研究进展

> 详见 `strategy_changelog.md`。

- **§3.4 复权 Bug 修复（2026-05-25，重要）** —— 旧 `qfq` 未连续化 corporate action，改为本地 **HFQ 后复权**。此前依赖价格数值的历史诊断不可信，结构性认知仍成立。

- **§3.5 新增 `fixed_cycle` 调仓模式（2026-06-02）** —— 候选模式，未替换实盘。

- **PTrade 迁移 + §3.6 rd=2 口径确立（2026-06-12）** —— 策略迁移到 PTrade 并跑通全周期回测（2014–2026，含真实佣金）。
  含成本回测 + 资产归因显示：全周期 rd=5 略胜（2016–2021 拉动），但**近 4.5 年 rd=2 收益/Sharpe/回撤三项全优且广谱**（黄金+创业板时机更优，非单一资产运气）。
  → **实盘基准锁定 rd=2**，弥合了长期悬而未决的 rd=5/rd=2 口径不一致；并设立「天气预报员」监测协议。

---

## 四、待办 / 风险点

1. **🟡 「天气预报员」rd=5 影子组合脚本（落地监测协议）**
   §3.6 的监测协议目前是文档约定，靠人记着对比不可靠。需写脚本：复用回测引擎，定期（周/月）用最新数据同时算 rd=2（实盘账本）与 rd=5（影子）滚动净值，自动输出三类信号读数（whipsaw / 滚动回撤 / 影子相对表现），达阈值钉钉预警。**这是把 regime 监测自动化的关键一步。**

2. **🟡 定夺 rebalance_days（rd=1 vs 2 vs 5）@ 真实佣金万0.5**
   实盘 ETF 佣金为**万0.5**（回测早先用万2 高估了摩擦），更高换手在实盘可接受。
   - ✅ **本地引擎交叉验证已完成**（2026-06-12，`commission_ratio=0.00005`）：真实费率下 **rd=1 > rd=2 > rd=5**（CAGR 33.81 / 32.73 / 31.64%，Sharpe 1.29 / 1.26 / 1.23），近3年 rd=1 收益/Sharpe/回撤三项全优。归因广谱（创业板+黄金+纳指）。结果见 `backtest/ptrade/README.md` §五。
   - ⏳ **待办（周一）：在 PTrade 跑 `c05_rd1/2/5`（均万0.5）确认**——国金平台周末不可回测。PTrade 为权威口径。
   - rd=1 若在 PTrade 复现胜出 → 走 §3.6 流程立 **§3.7** 条目，将实盘基准 rd=2 → rd=1，并据 rd=1 高换手**收紧「天气预报员」whipsaw 阈值**（rd=1 无最短持有期，回撤最深、最吃 regime）。

3. **✅ 已完成：§3.2 交易成本模型（框架侧）** —— `backtest/runner.py` 已加 `commission_ratio` 参数（默认 0，按 `turnover = Σ|Δweight|` 在开盘成交日扣单/双边佣金），含测试。框架侧现可跑含成本回测；用于本周末的本地 rd=1/2/5 交叉验证。

4. **🟡 滞后阈值扫描（Hysteresis threshold scan）** —— 设计稿与实施计划在 `docs/superpowers/plans/2026-05-22-...`，尚未落地。

5. **✅ 已解决：rebalance_days 口径不一致** —— 由 §3.6 含成本回测有据确立 rd=2，闭环。

6. **🟡 通知渠道：对接飞书 / Telegram** —— 钉钉用不习惯，后续切换。通知层是可插拔适配器（DESIGN §2.6，统一 `send(message)` 接口），新增飞书或 Telegram 适配器各一个文件即可，再把 `run_daily.py` / `backfill_ytd.py` 的推送切到新渠道。

---

## 五、下一步计划

1. **周一：PTrade 跑 `c05_rd1/2/5`（万0.5）确认**（本地引擎已指向 rd=1）→ 定夺最终调仓窗口，立 §3.7
2. **落地「天气预报员」影子脚本** → regime 监测自动化
3. PTrade 转入模拟盘 → 实盘
